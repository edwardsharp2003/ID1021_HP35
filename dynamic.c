#include <stdlib.h>
#include <stdio.h>

void push(stack *stk, int val) {
    if (stk->top == stk->size) {
        int size =
        stk->size =
        int *copy =
        for (int i = 0; i < ...; i++) {
            copy[i] =
            }
        free(stk->array);
        stk->array = copy;
    }
    // insert code
}
